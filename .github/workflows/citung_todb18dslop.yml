name: citung_todb18dslop
on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-22.04
    steps:
      - name: ssh to vm todb18
        uses: appleboy/ssh-action@v1.0.3
        with:
          key: ${{ secrets.SSH_PRIVATEKEY_TUNG }}
          username: tung
          host: 52.221.30.212
          script: |
            printf '\n--- Pull latest code\n'
            git fetch --all
            git switch -C tung origin/tung

            printf '\n--- Access to docker-compose directory\n'
            cd /home/tung/todb18_danhsachlop/docker-learn/tung_practice/dockerize_python/webapp_gitcloned/flask_webapp/docker

            printf '\n--- Run compose down to start with clean environment\n'
            ./compose_down.sh

            printf '\n--- Run compose up to start or update flask apiapp\n'
            ./compose_buildup.sh

            printf '\n--- Test flask apiapp endpoint\n'
            curl http://localhost:33400/
